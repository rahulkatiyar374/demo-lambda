service: express-lambda-app

provider:
  name: aws
  runtime: nodejs18.x
  region: ap-south-1
  stage: prod
  httpApi:
    cors: true

functions:
  api:
    handler: index.handler
    events:
      - httpApi: '*'
